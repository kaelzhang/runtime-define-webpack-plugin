const test = require('ava')
const log = require('util').debuglog('runtime-define-webpack-plugin')
const runtime_define_webpack_plugin = require('../src')

test('description', t => {
  t.is(true, true)
})

const test = require('ava')
const log = require('util').debuglog('runtime-environment-webpack-plugin')
const RuntimeEnvironmentPlugin = require('../src')

test('description', t => {
  t.is(true, true)
})
